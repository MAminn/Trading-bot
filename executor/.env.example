# Allowed values: OFF, TESTNET_READ, TESTNET_TRADE, LIVE_READ, LIVE_DRYRUN, LIVE
# WARNING: only OFF and TESTNET_READ are implemented in this build —
# any other value is refused and the executor exits with code 1.
EXECUTION_MODE=OFF

# Testnet-only credentials from https://testnet.binancefuture.com (never use live keys here).
# Required when EXECUTION_MODE=TESTNET_READ.
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=

# App API used by the signal consumer (required when EXECUTION_MODE=TESTNET_READ).
# Base URL of the Node app, e.g. https://your-app.example.com
APP_API_BASE=
# Bearer token for the app's /api/public/engine/* endpoints (must match the app's ENGINE_SERVICE_TOKEN).
ENGINE_SERVICE_TOKEN=
# UUID of the user whose signals this executor consumes.
ENGINE_USER_ID=
